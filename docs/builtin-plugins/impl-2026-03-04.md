# 内置插件管理 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现 100 个内置插件 + 用户通过 Manager/git clone 安装到 NAS + 用户插件优先于内置插件 + 版本升级时自动安装内置插件依赖。

**Architecture:** 采用 Delta Directory 模式。`comfyui/custom_nodes` 保持为指向 NAS 的软链接（天然为 `folder_paths[0]`，Manager 安装目标）。在容器内维护 `built-in/custom_nodes_delta`，仅包含"非冲突内置插件"的软链接，通过 `extra_model_paths.yaml` 追加为 `[1]`。NAS 无需额外配置。

**Tech Stack:** Python 3.10, ComfyUI extra_model_paths.yaml, pytest, Docker multi-stage build

**Design Doc:** `docs/builtin-plugins/design-2026-03-04.md`

---

## Part 1: 内置插件运行时管理 (Tasks 1-3)

### Task 1: 新建 builtin_custom_nodes.py 及其测试

**Files:**
- Create: `src/code/agent/services/custom_nodes/builtin_custom_nodes.py`
- Create: `src/code/agent/test/unit/services/custom_nodes/builtin_custom_nodes_test.py`

**Step 1: 编写失败测试**

创建 `src/code/agent/test/unit/services/custom_nodes/builtin_custom_nodes_test.py`，包含以下测试用例：

- `test_prepares_delta_dir`: 确保 Delta 目录被创建（若存在则重建）
- `test_links_builtin_not_in_nas`: NAS 没有的内置插件创建软链接到 Delta
- `test_skips_builtin_already_in_nas`: NAS 已有的内置插件不创建软链接（用户优先）
- `test_case_insensitive_exclusion`: 排除逻辑忽略大小写（ComfyUI-Plugin vs comfyui-plugin）
- `test_writes_extra_model_paths`: 写入 extra_model_paths.yaml，追加 Delta 目录为 `[1]`（NAS 无需配置）
- `test_nas_dir_created_if_missing`: NAS 目录不存在时自动创建

**Step 2: 运行测试，确认失败**

```bash
cd src/code/agent && python -m pytest test/unit/services/custom_nodes/builtin_custom_nodes_test.py -v 2>&1 | head -30
```

**Step 3: 实现 builtin_custom_nodes.py**

创建 `src/code/agent/services/custom_nodes/builtin_custom_nodes.py`：

```python
"""
内置插件 Delta Directory 管理模块

采用 Delta Directory 模式：
1. 维护 /root/built-in/custom_nodes_delta/，仅包含用户 NAS 中不存在的内置插件的软链接。
2. 配置 extra_model_paths.yaml，追加 Delta 目录，使内置插件在用户插件之后加载。
"""

import os
import shutil

from utils.logger import log
import constants

BUILTIN_NODES_DIR = os.getenv("BUILTIN_NODES_DIR", "/root/built-in/custom_nodes")
BUILTIN_DELTA_DIR = os.getenv("BUILTIN_DELTA_DIR", "/root/built-in/custom_nodes_delta")


def setup_builtin_custom_nodes(
    comfyui_dir: str = constants.COMFYUI_DIR,
    user_nodes_dir: str = None,
    builtin_src_dir: str = BUILTIN_NODES_DIR,
    builtin_delta_dir: str = BUILTIN_DELTA_DIR,
) -> None:
    if user_nodes_dir is None:
        user_nodes_dir = os.path.join(constants.MNT_DIR, "custom_nodes")

    log("INFO", "[BuiltinCustomNodes] Setting up Delta Directory...")

    # 步骤 1：准备 Delta 目录
    if os.path.exists(builtin_delta_dir):
        shutil.rmtree(builtin_delta_dir)
    os.makedirs(builtin_delta_dir)

    # 步骤 2：扫描用户插件目录 (大小写不敏感)
    os.makedirs(user_nodes_dir, exist_ok=True)
    user_node_names = set()
    try:
        user_node_names = {
            name.lower()
            for name in os.listdir(user_nodes_dir)
            if os.path.isdir(os.path.join(user_nodes_dir, name))
        }
    except Exception as e:
        log("ERROR", f"[BuiltinCustomNodes] Failed to scan user nodes dir: {e}")

    # 步骤 3：填充 Delta 目录 (排除与用户插件冲突的内置插件)
    if not os.path.isdir(builtin_src_dir):
        log("WARNING", f"[BuiltinCustomNodes] Built-in dir not found: {builtin_src_dir}")
    else:
        linked = skipped = 0
        for name in os.listdir(builtin_src_dir):
            if name.startswith(".") or name.startswith("__"):
                continue

            if name.lower() in user_node_names:
                skipped += 1
                continue

            src_path = os.path.join(builtin_src_dir, name)
            if not os.path.isdir(src_path):
                continue

            link_path = os.path.join(builtin_delta_dir, name)
            try:
                os.symlink(src_path, link_path)
                linked += 1
            except Exception as e:
                log("ERROR", f"[BuiltinCustomNodes] Failed to link {name}: {e}")

        log(
            "INFO",
            f"[BuiltinCustomNodes] Delta dir ready: {linked} linked, {skipped} skipped (user overrides)",
        )

    # 步骤 4：写入 extra_model_paths.yaml
    _write_config(comfyui_dir, builtin_delta_dir)


def _write_config(comfyui_dir: str, builtin_delta_dir: str) -> None:
    extra_paths_file = os.path.join(comfyui_dir, "extra_model_paths.yaml")
    delta_base = os.path.dirname(builtin_delta_dir)  # /root/built-in
    delta_name = os.path.basename(builtin_delta_dir)  # custom_nodes_delta

    # NAS 天然是 folder_paths[0]（ComfyUI 默认初始化），无需额外配置
    # 只追加 Delta 目录作为 [1]
    yaml_block = (
        f"\nfunart_builtin_custom_nodes:\n"
        f"    base_path: {delta_base}\n"
        f"    custom_nodes: {delta_name}/\n"
    )

    if not os.path.exists(extra_paths_file):
        with open(extra_paths_file, "w") as f:
            f.write(yaml_block.lstrip())
    else:
        with open(extra_paths_file) as f:
            content = f.read()
        if "funart_builtin_custom_nodes" not in content:
            with open(extra_paths_file, "a") as f:
                f.write(yaml_block)
```

**Step 4: 运行测试，确认通过**

```bash
cd src/code/agent && python -m pytest test/unit/services/custom_nodes/builtin_custom_nodes_test.py -v
```

**Step 5: 提交**

```bash
git add src/code/agent/services/custom_nodes/builtin_custom_nodes.py \
        src/code/agent/test/unit/services/custom_nodes/builtin_custom_nodes_test.py
git commit -m "feat: add builtin_custom_nodes module with delta directory logic"
```

---

### Task 2: 修改 management_service.py

**Files:**
- Modify: `src/code/agent/services/management_service.py`

**Step 1: 在 start() 中调用 setup_builtin_custom_nodes()**

```python
if constants.BACKEND_TYPE == constants.TYPE_COMFYUI:
    from services.custom_nodes.builtin_custom_nodes import setup_builtin_custom_nodes
    from services.model.shared_models import setup_shared_models
    setup_builtin_custom_nodes()   # 新增
    setup_shared_models()
```

**Step 2: 运行测试**

```bash
cd src/code/agent && python -m pytest test/unit/services/management_service_test.py -v
```

**Step 3: 提交**

```bash
git add src/code/agent/services/management_service.py
git commit -m "feat: call setup_builtin_custom_nodes on startup"
```

---

### Task 3: 修改 Dockerfile

**Files:**
- Modify: `src/code/comfyui/Dockerfile`（或 `src/code/agent/Dockerfile`，视构建方式而定）

**Step 1: 声明环境变量**

```dockerfile
ENV BUILTIN_NODES_DIR="/root/built-in/custom_nodes"
ENV BUILTIN_DELTA_DIR="/root/built-in/custom_nodes_delta"
```

**Step 2: 验证构建**

```bash
docker build -t comfyui:test -f src/code/comfyui/Dockerfile src/
```

**Step 3: 提交**

```bash
git add src/code/comfyui/Dockerfile
git commit -m "feat: add delta directory env vars to Dockerfile"
```

---

## Part 2: 内置插件列表筛选工具 (Task 4)

### Task 4: 新建 get_topn.py（插件 Top N 筛选脚本）

**Files:**
- Create: `src/code/comfyui/custom_nodes_config/get_topn.py`

**背景：** 此脚本用于从 ComfyUI Manager 官方数据中按 star 数筛选 top N 插件，生成供人工审核的候选列表，最终由人工确认并重命名为 `custom_nodes.json`。

**Step 1: 实现 get_topn.py**

创建 `src/code/comfyui/custom_nodes_config/get_topn.py`：

```python
"""
ComfyUI 内置插件 Top N 筛选脚本

用法：
    python get_topn.py --top 100
    python get_topn.py --top 50 --output-dir ./

说明：
    1. 从 ComfyUI Manager GitHub 下载 github-stats.json（star 数）和 custom-node-list.json（插件列表）
    2. 合并数据，按 star 数降序排序
    3. 排除 excluded_custom_nodes.json 中的插件（repository URL 完全匹配）
    4. 取前 N 个，写入 custom_nodes_topn_YYYY-MM-DD.json

输出文件供人工审核，确认后重命名为 custom_nodes.json 作为 Dockerfile 安装依据。
"""

import argparse
import json
import os
from datetime import date

import requests

STATS_URL = "https://raw.githubusercontent.com/Comfy-Org/ComfyUI-Manager/main/github-stats.json"
LIST_URL = "https://raw.githubusercontent.com/Comfy-Org/ComfyUI-Manager/main/custom-node-list.json"

SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))
EXCLUDED_FILE = os.path.join(SCRIPT_DIR, "excluded_custom_nodes.json")


def fetch_json(url: str) -> dict:
    """下载 JSON 数据"""
    resp = requests.get(url, timeout=30)
    resp.raise_for_status()
    return resp.json()


def merge_and_rank(stats: dict, node_list: dict) -> list:
    """合并 stats 和 node_list，按 star 数降序排序"""
    result = []
    for node in node_list.get("custom_nodes", []):
        repo_url = node.get("reference", "")
        star_data = stats.get(repo_url, {})
        stars = star_data.get("stars", 0) if isinstance(star_data, dict) else 0
        result.append({
            "id": _url_to_id(repo_url),
            "name": node.get("title", ""),
            "repository": repo_url,
            "stars": stars,
            "enabled": True,
            "description": node.get("description", ""),
        })
    result.sort(key=lambda x: x["stars"], reverse=True)
    return result


def filter_excluded(ranked: list, excluded: dict) -> list:
    """排除 excluded 列表中的插件（repository URL 完全匹配）"""
    excluded_repos = {
        node["repository"]
        for node in excluded.get("custom_nodes", [])
        if "repository" in node
    }
    return [item for item in ranked if item["repository"] not in excluded_repos]


def generate_output(items: list, top_n: int, output_dir: str, excluded_count: int = 0) -> str:
    """生成 top N 输出文件，返回文件路径"""
    today = date.today().isoformat()
    filename = f"custom_nodes_topn_{today}.json"
    output_path = os.path.join(output_dir, filename)

    output = {
        "metadata": {
            "generated_at": today,
            "top_n": top_n,
            "total": len(items),
            "source_stats_url": STATS_URL,
            "source_list_url": LIST_URL,
            "excluded_count": excluded_count,
        },
        "custom_nodes": items,
    }
    with open(output_path, "w", encoding="utf-8") as f:
        json.dump(output, f, ensure_ascii=False, indent=2)
    return output_path


def get_topn_plugins(top_n: int = 100, output_dir: str = SCRIPT_DIR) -> str:
    """主流程：下载数据 → 合并排序 → 过滤排除 → 写文件"""
    print(f"[get_topn] 下载 github-stats.json ...")
    stats = fetch_json(STATS_URL)

    print(f"[get_topn] 下载 custom-node-list.json ...")
    node_list = fetch_json(LIST_URL)

    print(f"[get_topn] 合并数据，共 {len(node_list.get('custom_nodes', []))} 个插件")
    ranked = merge_and_rank(stats, node_list)

    excluded = {"custom_nodes": []}
    if os.path.exists(EXCLUDED_FILE):
        with open(EXCLUDED_FILE, encoding="utf-8") as f:
            excluded = json.load(f)
    excluded_count = len(excluded.get("custom_nodes", []))

    filtered = filter_excluded(ranked, excluded)
    top_items = filtered[:top_n]

    print(f"[get_topn] 排除 {excluded_count} 个，取 top {top_n}（实际 {len(top_items)} 个）")
    output_path = generate_output(top_items, top_n=top_n, output_dir=output_dir, excluded_count=excluded_count)
    print(f"[get_topn] 已写入: {output_path}")
    return output_path


def _url_to_id(url: str) -> str:
    """将 repository URL 转换为 id（取 owner/repo 格式的 repo 部分）"""
    return url.rstrip("/").split("/")[-1].lower() if url else ""


if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="生成 ComfyUI 内置插件 Top N 候选列表")
    parser.add_argument("--top", type=int, default=100, help="取前 N 个（默认 100）")
    parser.add_argument("--output-dir", type=str, default=SCRIPT_DIR, help="输出目录（默认脚本所在目录）")
    args = parser.parse_args()
    get_topn_plugins(top_n=args.top, output_dir=args.output_dir)
```

**Step 2: 手动验证脚本可运行（需要网络）**

```bash
cd src/code/comfyui/custom_nodes_config
pip install requests  # 如未安装
python get_topn.py --top 100
# 预期输出：custom_nodes_topn_YYYY-MM-DD.json
```

**Step 3: 提交**

```bash
git add src/code/comfyui/custom_nodes_config/get_topn.py
git commit -m "feat: add get_topn script for star-based plugin selection"
```

---

## Part 3: 内置插件版本升级与依赖兼容性 (Tasks 5-8)

**背景：** 用户从 v1 升级到 v2（首次引入内置插件），或内置插件列表迭代时，用户 NAS 快照中的 venv 缺少内置插件所需的 pip 依赖，导致 ComfyUI 启动失败。通过"版本标记 + 启动时按需安装"解决。

---

### Task 5: 扩展 builtin_custom_nodes.py：版本检查与依赖安装

**Files:**
- Modify: `src/code/agent/services/custom_nodes/builtin_custom_nodes.py`
- Modify: `src/code/agent/test/unit/services/custom_nodes/builtin_custom_nodes_test.py`

**Step 1: 补充失败测试**

在现有测试文件中新增以下测试用例：

- `test_get_builtin_version_reads_file`: 从 `/root/built-in/version.txt` 读取版本号
- `test_get_builtin_version_returns_unknown_if_missing`: 文件不存在时返回 `"unknown"`
- `test_get_installed_dependency_version_from_nas`: 从 NAS 标记文件读取已安装版本
- `test_get_installed_dependency_version_returns_empty_if_missing`: NAS 文件不存在时返回 `""`（触发安装）
- `test_install_builtin_dependencies_skips_when_version_matches`: 版本一致时不调用 PIPInstaller
- `test_install_builtin_dependencies_installs_when_version_differs`: 版本不一致时调用 PIPInstaller 并更新 NAS 标记
- `test_install_builtin_dependencies_does_not_update_version_on_failure`: 安装失败时不更新 NAS 标记（下次启动重试）
- `test_save_installed_dependency_version_tolerates_write_error`: NAS 不可写时捕获异常，不中断流程

**Step 2: 运行测试，确认失败**

```bash
cd src/code/agent && python -m pytest test/unit/services/custom_nodes/builtin_custom_nodes_test.py -v 2>&1 | head -40
```

**Step 3: 在 builtin_custom_nodes.py 中追加版本管理函数**

```python
BUILTIN_VERSION_FILE = os.path.join(os.path.dirname(BUILTIN_NODES_DIR), "version.txt")
INSTALLED_VERSION_FILE = os.path.join(constants.MNT_DIR, ".funart", "dependency_version.txt")


def get_builtin_version() -> str:
    """读取镜像内嵌的内置版本号"""
    try:
        with open(BUILTIN_VERSION_FILE) as f:
            return f.read().strip()
    except Exception:
        return "unknown"


def get_installed_dependency_version() -> str:
    """读取 NAS 上记录的已安装版本号，不存在时返回空字符串（视为需要安装）"""
    try:
        with open(INSTALLED_VERSION_FILE) as f:
            return f.read().strip()
    except Exception:
        return ""


def _save_installed_dependency_version(version: str) -> None:
    """安装完成后将版本号持久化到 NAS"""
    try:
        os.makedirs(os.path.dirname(INSTALLED_VERSION_FILE), exist_ok=True)
        with open(INSTALLED_VERSION_FILE, "w") as f:
            f.write(version)
    except Exception as e:
        log("ERROR", f"[BuiltinCustomNodes] Failed to save installed version: {e}")


def install_builtin_dependencies() -> bool:
    """
    检查内置插件版本，按需安装依赖到用户 venv。

    Returns:
        bool: True 表示执行了安装，False 表示版本一致跳过了
    """
    builtin_ver = get_builtin_version()
    installed_ver = get_installed_dependency_version()

    if builtin_ver == installed_ver:
        log("INFO", f"[BuiltinCustomNodes] Dependencies up-to-date (version={builtin_ver}), skipping install.")
        return False

    log("INFO", f"[BuiltinCustomNodes] Version changed: {installed_ver!r} → {builtin_ver!r}. Installing dependencies...")

    if not os.path.isdir(BUILTIN_NODES_DIR):
        log("WARNING", f"[BuiltinCustomNodes] Built-in dir not found: {BUILTIN_NODES_DIR}, skip install.")
        return False

    from services.pip.pip_installer import PIPInstaller
    installer = PIPInstaller()
    # 传入内置节点目录，复用现有安装逻辑（包括冲突解析、两轮重试等）
    installer.install_all(nodes_custom_dir=BUILTIN_NODES_DIR)

    # 安装完成后更新 NAS 版本标记；安装失败不更新，下次启动会重试
    _save_installed_dependency_version(builtin_ver)
    log("INFO", f"[BuiltinCustomNodes] Dependencies installed, version saved: {builtin_ver}")
    return True
```

**Step 4: 运行测试，确认通过**

```bash
cd src/code/agent && python -m pytest test/unit/services/custom_nodes/builtin_custom_nodes_test.py -v
```

**Step 5: 提交**

```bash
git add src/code/agent/services/custom_nodes/builtin_custom_nodes.py \
        src/code/agent/test/unit/services/custom_nodes/builtin_custom_nodes_test.py
git commit -m "feat: add version tracking and dependency install to builtin_custom_nodes"
```

---

### Task 6: 扩展 pip_installer.py：支持自定义节点目录

**Files:**
- Modify: `src/code/agent/services/pip/pip_installer.py`
- Modify: `src/code/agent/test/unit/services/pip/pip_installer_test.py`

**Step 1: 补充失败测试**

新增测试用例：

- `test_install_all_uses_custom_nodes_dir`: 传入 `nodes_custom_dir` 时，从该目录扫描节点而非默认 `comfyui/custom_nodes`

**Step 2: 运行测试，确认失败**

```bash
cd src/code/agent && python -m pytest test/unit/services/pip/pip_installer_test.py -v -k "custom_nodes_dir" 2>&1 | head -20
```

**Step 3: 修改 pip_installer.py**

在 `install_all` 方法签名中增加 `nodes_custom_dir` 参数：

```python
def install_all(
    self,
    timeout: int = constants.DEFAULT_INSTALL_TIMEOUT,
    nodes_map: Optional[Dict] = None,
    nodes_custom_dir: Optional[str] = None,
) -> Dict:
    """
    nodes_custom_dir: 若不为 None，从该目录（而非默认 comfyui/custom_nodes）扫描节点。
                      用于安装内置插件（built-in）的依赖。
    """
    nodes_path = nodes_custom_dir or os.path.join(constants.COMFYUI_DIR, "custom_nodes")
    all_available_nodes = self._get_possible_nodes(nodes_path)
    # 其余逻辑不变
    ...
```

**Step 4: 运行全量 pip 相关测试**

```bash
cd src/code/agent && python -m pytest test/unit/services/pip/ -v
```

**Step 5: 提交**

```bash
git add src/code/agent/services/pip/pip_installer.py \
        src/code/agent/test/unit/services/pip/pip_installer_test.py
git commit -m "feat: add nodes_custom_dir param to pip_installer.install_all"
```

---

### Task 7: 修改 management_service.py：调用 install_builtin_dependencies()

**Files:**
- Modify: `src/code/agent/services/management_service.py`

**Step 1: 在 start() 中追加调用**

将现有代码从：

```python
if constants.BACKEND_TYPE == constants.TYPE_COMFYUI:
    from services.custom_nodes.builtin_custom_nodes import setup_builtin_custom_nodes
    from services.model.shared_models import setup_shared_models
    setup_builtin_custom_nodes()
    setup_shared_models()
```

修改为：

```python
if constants.BACKEND_TYPE == constants.TYPE_COMFYUI:
    from services.custom_nodes.builtin_custom_nodes import (
        setup_builtin_custom_nodes,
        install_builtin_dependencies,
    )
    from services.model.shared_models import setup_shared_models
    setup_builtin_custom_nodes()
    setup_shared_models()
    # 版本变化时自动安装内置插件依赖，与 nodes_map 无关，每次启动都执行版本检查
    install_builtin_dependencies()
```

**Step 2: 运行测试**

```bash
cd src/code/agent && python -m pytest test/unit/services/management_service_test.py -v
```

**Step 3: 提交**

```bash
git add src/code/agent/services/management_service.py
git commit -m "feat: call install_builtin_dependencies on startup for version-aware dep install"
```

---

### Task 8: 修改 Dockerfile：写入内置插件版本文件

**Files:**
- Modify: `src/code/agent/Dockerfile`（或构建脚本）

**Step 1: 在 built-in 目录写入版本号**

在 agent 镜像的构建阶段，写入版本文件：

```dockerfile
# 写入内置插件版本号，供启动时版本检查使用
# 版本号需随内置插件列表的实质性变化（增删插件、升级版本）而更新
RUN echo "v2.0.0" > /root/built-in/version.txt
```

> **版本号维护规则：** 内置插件列表（`custom_nodes.json`）发生实质性变化（增删插件、升级插件 commit）时，需同步更新此版本号，以触发存量用户在下次启动时自动重新安装依赖。

**Step 2: 验证构建**

```bash
docker build -t agent:test -f src/code/agent/Dockerfile src/
docker run --rm agent:test cat /root/built-in/version.txt
# 预期输出: v2.0.0
```

**Step 3: 提交**

```bash
git add src/code/agent/Dockerfile
git commit -m "feat: embed builtin version file for dependency version tracking"
```
